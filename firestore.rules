rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all authenticated and guest users to read/write
    // For production app - allows gameplay for all users
    match /{document=**} {
      allow read, write: if true;  // Open access for all users
    }
  }
}
